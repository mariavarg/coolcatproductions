Flask==2.3.3
python-dotenv==1.0.0
Werkzeug==2.3.7
stripe==5.4.0  # Downgraded to a more stable version
pyotp==2.8.0
qrcode==7.4.2  # Removed the [pil] extra
Pillow==9.5.0  # Explicitly added Pillow for image support
